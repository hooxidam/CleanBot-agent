# 复制此文件为 .env 并填入你自己的密钥
# 阿里云百炼(DashScope) 控制台获取：https://bailian.console.aliyun.com/
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 说明：本项目的模型名、向量库路径等配置都在 config/*.yml 里（config/agent.yml、
# config/rag.yml、config/chroma.yml），不通过环境变量注入；数据库路径由代码固定在
# 项目根目录（app.sqlite、chroma_db/）。
# 因此这里只需要配置上面这一个密钥。
#
# Docker 部署时，本文件由 docker-compose.yml 的 env_file 注入容器，
# 不会被打包进镜像（见 .dockerignore），密钥不会留在镜像层里。